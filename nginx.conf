server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;

    # Assets hashés par Create React App : cache long
    location /static/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # index.html jamais mis en cache, sinon un déploiement passe inaperçu
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    # Fallback SPA (React Router en BrowserRouter)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
